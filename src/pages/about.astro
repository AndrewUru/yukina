---
import { getEntry, render } from "astro:content";
import Main from "../layouts/MainLayout.astro";
import CopyRight from "../components/misc/CopyRight.astro";

const aboutPost = await getEntry("specs", "about");
const { Content } = await render(aboutPost!);

interface Props {
  title?: string;
  subTitle?: string;
  bannerImage?: string;
  published?: Date;
}

const { title, subTitle, bannerImage, published } = Astro.props;
---

<Main title={title} subTitle={subTitle} bannerImage={bannerImage}>
  <div
    class="article-wrapper rounded-3xl bg-gradient-to-br from-purple-100 to-indigo-100 p-8 shadow-xl dark:from-gray-800 dark:to-gray-900"
  >
    <div class="article mx-auto max-w-3xl space-y-12">
      <!-- Sobre Alva Fusión Designs -->
      <section
        class="rounded-xl border border-purple-200 bg-white p-8 shadow-lg dark:border-gray-700 dark:bg-gray-900"
      >
        <h2
          class="mb-6 text-center text-3xl font-bold text-[var(--color-primary)] dark:text-white"
        >
          Sobre Alva Fusión Designs
        </h2>
        <p class="text-lg leading-relaxed text-gray-700 dark:text-gray-300">
          Alva Fusión Designs nace en 2022 como un proyecto madre-hija con el
          propósito de compartir nuestra pasión por el tejido artesanal. Cada
          creación está hecha con amor, cuidado, sostenibilidad y calidad.
        </p>
        <p class="mt-6 leading-relaxed text-gray-700 dark:text-gray-300">
          Mi madre, con años de experiencia en tejido a ganchillo, dos agujas y
          telares, ha sido mi inspiración y guía en este camino. Ahora, juntas
          creamos piezas únicas que combinan tradición y modernidad, llevando
          calidez y amor a cada hogar.
        </p>
        <p
          class="mt-6 text-center text-lg font-semibold text-gray-900 dark:text-white"
        >
          Te invitamos a ser parte de nuestra historia y descubrir la magia del
          tejido artesanal.
        </p>
      </section>

      <!-- Sección de Agradecimientos -->
      <section
        class="rounded-xl border border-purple-200 bg-white p-8 shadow-lg dark:border-gray-700 dark:bg-gray-900"
      >
        <h2
          class="mb-6 text-center text-2xl font-semibold text-[var(--color-primary)] dark:text-white"
        >
          Agradecimientos
        </h2>
        <p
          class="text-center text-lg leading-relaxed text-gray-700 dark:text-gray-300"
        >
          Queremos expresar nuestro agradecimiento a:
        </p>
        <ul class="mt-6 space-y-4 text-lg text-gray-700 dark:text-gray-300">
          <li class="flex items-center gap-2">
            <svg
              class="h-5 w-5 text-[var(--color-primary)] dark:text-[var(--color-accent)]"
              fill="currentColor"
              viewBox="0 0 20 20"
            >
              <circle cx="10" cy="10" r="8"></circle>
            </svg>
            <a
              href="https://www.elsaltoweb.es"
              class="text-[var(--color-primary)] hover:underline dark:text-[var(--color-accent)]"
              target="_blank"
            >
              El Salto Web
            </a>
            por su apoyo y contribución al desarrollo de nuestra plataforma.
          </li>
          <li class="flex items-center gap-2">
            <svg
              class="h-5 w-5 text-[var(--color-primary)] dark:text-[var(--color-accent)]"
              fill="currentColor"
              viewBox="0 0 20 20"
            >
              <circle cx="10" cy="10" r="8"></circle>
            </svg>
            <span
              >Noelia Bonifacio por su gestión de redes sociales y apoyo en
              nuestra comunidad.</span
            >
          </li>
        </ul>
      </section>

      {published && title && <CopyRight title={title} published={published} />}
    </div>
  </div>
</Main>

<style>
  .article-wrapper {
    @apply w-full rounded-3xl;
  }
  .article {
    @apply flex flex-col;
  }
</style>
