---
import { getEntry, render } from "astro:content";
import Main from "../layouts/MainLayout.astro";
import Markdown from "../components/Markdown.astro";
import CopyRight from "../components/misc/CopyRight.astro";

const aboutPost = await getEntry("specs", "about");
const { Content } = await render(aboutPost!);
interface Props {
  title?: string;
  subTitle?: string;
  bannerImage?: string;
  published?: Date;
}
const { title, subTitle, bannerImage, published } = Astro.props;
---

<Main title={title} subTitle={subTitle} bannerImage={bannerImage}>
  <!-- <Fragment set:html={tocHTMLString} /> -->
  <div class="article-wrapper">
    <div class="article">
      <Markdown>
            <h2 class="mb-6 text-[var(--color-text)] dark:text-white">
          Sobre Alva Fusión Designs
        </h2>
        <div
          class="rounded-lg bg-white p-6 text-center shadow-lg dark:bg-gray-900"
        >
          <p class="text-lg text-[var(--color-text)] dark:text-gray-300">
            Alva Fusión Designs nace en 2022 como un proyecto madre-hija con el
            propósito de compartir nuestra pasión por el tejido artesanal. Cada
            una de nuestras creaciones está hecha con amor, cuidado y un enfoque
            en la sostenibilidad y la calidad.
          </p>
          <p class="mt-4 text-[var(--color-text)] dark:text-gray-300">
            Mi madre, con años de experiencia en tejido a ganchillo, dos agujas
            y telares, ha sido mi inspiración y guía en este camino. Ahora,
            juntas creamos piezas únicas que combinan tradición y modernidad,
            llevando calidez y amor a cada hogar.
          </p>
          <p
            class="mt-4 font-semibold text-[var(--color-text)] dark:text-gray-100"
          >
            Te invitamos a ser parte de nuestra historia y descubrir la magia
            del tejido artesanal.
          </p>
        </div>

        <!-- Agradecimientos -->

        <h2 class="mb-6 font-semibold text-[var(--color-text)] dark:text-white">
          Agradecimientos
        </h2>
        <div class="rounded-lg bg-white p-6 shadow-lg dark:bg-gray-900">
          <p class="text-lg text-[var(--color-text)] dark:text-gray-300">
            Queremos expresar nuestro agradecimiento a:
          </p>
          <ul
            class="mt-4 space-y-2 text-[var(--color-text)] dark:text-gray-300"
          >
            <li>
              <a
                href="https://www.elsaltoweb.es"
                class="text-[var(--color-primary)] hover:underline dark:text-[var(--color-accent)]"
                target="_blank">El Salto Web</a
              > por su apoyo y contribución al desarrollo de nuestra plataforma.
            </li>
            <li>
              Noelia Bonifacio por su increíble gestión de redes sociales y
              apoyo en nuestra comunidad.
            </li>
          </ul>
        </div>
      </Markdown>
      {published && title && <CopyRight title={title} published={published} />}
    </div>
  </div>
</Main>

<style>
  .article-wrapper {
    @apply w-full rounded-2xl;
  }
  .article {
    @apply flex flex-col;
  }
</style>
